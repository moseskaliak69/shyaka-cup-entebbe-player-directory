{
  "name": "shyaka-cup-entebbe-2026",
  "version": "3.0.0",
  "private": true,
  "description": "Shyaka Cup Entebbe complete tournament app",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "android:add": "npm run mobile:prepare && npx cap add android",
    "ios:add": "npm run mobile:prepare && npx cap add ios",
    "sync": "npm run mobile:prepare && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
